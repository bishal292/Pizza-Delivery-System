export const AdminAuthSlice = (set) => (
    {
        adminInfo: undefined,
        setAdminInfo: (adminInfo) => set(() => ({ adminInfo })),
    }
)


export const adminUtilSlice = (set) => (
    {
        
    }
)